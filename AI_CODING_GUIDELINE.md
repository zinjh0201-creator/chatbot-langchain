🤖 [프로젝트 핵심] 사내 규정 Agentic RAG 챗봇 개발 매뉴얼
🎯 제1장. 챗봇의 존재 이유 및 핵심 기획 의도 (Core Intent)
본 챗봇은 단순한 '단어 검색기'가 아닌, 사용자의 맥락을 읽어내는 **'지능형 의도 파악 비서(Intent-driven Agent)'**이다. 모든 코드는 다음 3대 원칙을 절대적으로 준수하여 작성되어야 한다.

질문 의도 파악 우선 (Query Rewriting):

사용자가 일상어("오늘 아파서 쉰다")로 질문해도, 시스템 내부적으로 사내 공식 규정 용어("연차", "병가", "휴가 신청")로 통역 및 확장하는 로직이 반드시 검색 전에 선행되어야 한다.

철저한 문서 우선주의 (Document-First RAG):

사내 DB(PDF 등)에서 검색된 문서를 기반으로 답변하는 것을 제1원칙으로 한다. 검색된 데이터가 있다면 LLM의 사전 학습 지식(예: 근로기준법, 일반 상식)은 절대 섞지 말고 100% 사내 문서 내용만 요약하여 답변한다.

최후의 보루로서의 상식 (Fallback to General Knowledge):

검색 결과가 '0건'이거나 사내 문서만으로 답변을 구성할 수 없는 명확한 경우에만, "사내 규정에는 없지만 일반적인 상식으로는..."이라는 안내와 함께 유연하게 답변한다.

🛠️ 제2장. LangChain 백엔드 코드 작성 표준 (Coding Standards)

1. 파이프라인(LCEL) 분리 및 모듈화

하나의 거대한 함수 안에서 프롬프트 생성, 검색, 답변 생성을 모두 처리하지 않는다.

의도 파악(Intent Router) -> 문서 검색(Retriever) -> 답변 생성(Generator) 3단계를 명확히 분리하여 체인(Chain)을 구성한다.

2. 프롬프트(Prompt) 관리의 엄격성

프롬프트 템플릿은 하드코딩을 최소화하고, 수정이 용이하도록 분리하여 관리한다.

답변 생성 프롬프트에는 반드시 **"제공된 {context} 밖의 내용은 지어내지 말 것"**이라는 강력한 페널티 조항을 포함한다.

3. 데이터 흐름(Data Flow) 로깅 및 검증 (가장 중요 ⭐)

체인의 각 단계를 넘어갈 때 데이터가 유실되지 않는지 검증하는 로직을 필수적으로 포함한다.

특히 retriever가 찾은 Document 객체들이 답변 생성 LLM의 {context} 변수에 올바른 텍스트 형태로 주입되고 있는지 콘솔에 명확히 로깅(logger.info)하여 추적할 수 있게 한다. (빈 값 전달 방지)

4. 성능 및 예외 처리 (Performance & Error Handling)

무거운 LLM 호출을 남발하지 않는다. 속도 저하를 막기 위해 단순 재정렬 등은 가벼운 수식(RRF 등)을 활용한다.

LLM이 뻗거나 DB 연결이 끊기는 등 예외 상황 발생 시, 사용자에게 에러 코드를 노출하지 않고 부드러운 안내 메시지로 감싸서 반환한다.
